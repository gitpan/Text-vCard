Addressbook:

find()
	- get vcards matching specific criteria

vCard:

	- make nice interface for fn() and other single element/value
	  nodes.

overall:
	- better doc examples
	- see about parsing version 3 vcf files