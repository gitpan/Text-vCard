TODO:

More tests all round!

Adding addresses, emails and other elements belonging to a list

  $card->address_new(
    'street' => "74 Bayswater",
    'city' => "Ottawa",
    'type' => [ 'home', 'work' ],
  );
- or -
  my $new_address = $card->address_new;
  $new_address->street( "74 Bayswater" );
  $new_address->city("ottawa");
  ....


my $agents = $vcard->agent();

API for Text::vCard::Part::Binary

Work with Jay on: 

- write out vcards
- parse version 2.1 of vCard format
